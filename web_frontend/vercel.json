{
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "functions": {
    "api/seo.js": {
      "includeFiles": "dist/index.html"
    }
  },
  "routes": [
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/api/seo?path=/$1" }
  ]
}
